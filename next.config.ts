import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // No extra configuration needed for now.
  // Add image domains here if item photos are added later.
}

export default nextConfig
